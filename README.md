# data-integrity-refinery
A specialized Python tool for cleaning and validating messy AI training datasets.

In AI training, garbage data leads to garbage models. This tool is a 'Data Guard' that validates JSON outputs and normalizes them into CSV formats. It showcases my ability to perform quality control on AI-generated datasets.
